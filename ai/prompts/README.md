# AI Prompts Directory

This directory stores the system prompt templates and specialized role guidelines for each of FSP's agents.

## Design
- Every prompt is clearly structured to dictate the persona, formatting rules (such as markdown responses), negative constraints, and contextual awareness boundaries.
- Prompts are kept modular so they can be iterated on and optimized independently from the execution code.
